{
  "name": "talktorobots",
  "type": "module",
  "version": "1.0.0",
  "description": "how to talk to robots — plain-English answers to 'how do I actually use AI?'",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource/ubuntu": "^5.1.0",
    "@fontsource/ubuntu-mono": "^5.1.0",
    "@fontsource/young-serif": "^5.1.0",
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
